spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3307/sprinkle-test?useSSL=false
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect